[pytest]
testpaths = tests/python
# The Rust integration tests live at tests/*.rs / tests/common/*.
# pytest's default collector only picks up .py files, so there's
# no cross-contamination, but we pin `testpaths` for clarity and
# to make `pytest` from the crate root Just Work.
